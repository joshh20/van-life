// basename is the folder that the app will run under
// For instance, if set to the sub-directory https:/example.com/van-life/
// Then you would set the basename to /van-life/
export const basename = '/van-life/';